-- Historical placeholder. Version 0049 shipped in field databases that can
-- already have 40-51 recorded as applied, so AO cannot rely on this slot for
-- required schema. The real review per-harness rebuild lives in 0080.

-- +goose Up
SELECT 1;

-- +goose Down
SELECT 1;
