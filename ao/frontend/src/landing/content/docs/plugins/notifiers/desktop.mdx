---
title: Desktop notifications
description: Legacy notifier-plugin URL; current notifications are built into the AO desktop app.
---

The standalone `desktop` notifier and `ao setup desktop` command are not supported by the Go rewrite. AO now stores durable notification records in the daemon and shows them through the [notification center](/docs/plugins/notifiers/dashboard), with Electron toasts while the app is running.
