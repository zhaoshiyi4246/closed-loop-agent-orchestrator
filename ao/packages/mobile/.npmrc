# Expo Router declares optional web/RSC peers. This native app does not ship the
# web target, and installing those peers pulls React versions incompatible with
# the terminal WebView package's exact React 19.1.0 peer.
legacy-peer-deps=true
