{
  "mission_id": "MISSION-QUICK-003",
  "project_id": "closed-loop-demo",
  "objective": "在 app.py 中新增函数 square(a) 返回 a*a；在 math2.py 中新增函数 cube(a) 返回 a*a*a。各配一个 pytest 测试（tests/test_square.py、tests/test_cube.py），全部测试通过。",
  "allowed_paths": ["app.py", "math2.py", "tests/test_square.py", "tests/test_cube.py"],
  "forbidden_paths": [".git/**", "*.md"],
  "acceptance_criteria": [
    {"id": "QAC-01", "description": "square(4) 返回 16"},
    {"id": "QAC-02", "description": "square(-3) 返回 9"},
    {"id": "QAC-03", "description": "cube(2) 返回 8"},
    {"id": "QAC-04", "description": "cube(-3) 返回 -27"}
  ],
  "gate_commands": ["python -m pytest -q"],
  "user_instruction": "两个函数都很小，一次性完成实现和测试，测试全绿即完成。不要修改任何已有文件。",
  "worker_harness": "claude-code",
  "budgets": {
    "max_subtasks": 2,
    "max_total_replans": 2,
    "max_runtime_seconds": 900,
    "subtask_budgets": {
      "max_local_fixes": 1,
      "max_replans": 1,
      "max_same_alerts": 2,
      "max_runtime_seconds": 250
    }
  }
}
