{
  "mission_id": "MISSION-DEMO-001",
  "project_id": "closed-loop-demo",
  "objective": "在仓库中实现两个新函数：divide(a,b)（除数为 0 抛 ValueError）放在 app.py；multiply(a,b) 放在新文件 math2.py。两个函数都要有对应测试且全部通过。",
  "allowed_paths": ["app.py", "math2.py", "tests/test_divide.py", "tests/test_multiply.py"],
  "forbidden_paths": [".git/**", "*.md"],
  "acceptance_criteria": [
    {"id": "MAC-01", "description": "divide(6,3) 返回 2"},
    {"id": "MAC-02", "description": "divide(1,0) 抛出 ValueError"},
    {"id": "MAC-03", "description": "multiply(2,3) 返回 6"},
    {"id": "MAC-04", "description": "multiply(-2,3) 返回 -6"}
  ],
  "gate_commands": ["python -m pytest -q"],
  "user_instruction": "一次性完成整个任务：divide 和 multiply 都要实现并有测试。测试全绿即完成。不得修改已有的 tests/test_divide.py 之外的验收逻辑。",
  "worker_harness": "claude-code",
  "budgets": {
    "max_subtasks": 3,
    "max_total_replans": 3,
    "max_runtime_seconds": 3600,
    "subtask_budgets": {
      "max_local_fixes": 2,
      "max_replans": 1,
      "max_same_alerts": 2,
      "max_runtime_seconds": 3600
    }
  }
}
